{% extends 'community/email/base-email.html.twig' %}

{% block content %}
    {% set url = url('sulu_community.password_reset', { token: user.passwordResetToken }) %}
    <mj-hero padding-top="50px" padding-bottom="50px" mode="fluid-height">
        <mj-text align="center" font-size="15px"> Pour réinitialiser votre mot de passe cliquez sur le bouton ci-dessous </mj-text>
        <mj-button background-color="#0E5C63" href="{{ url }}" font-size="15px" font-weight="900">
        Réinitialiser mon mot de passe
        </mj-button>
    </mj-hero>
{% endblock %}